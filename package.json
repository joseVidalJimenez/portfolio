{
  "name": "portfolio-tests",
  "version": "1.0.0",
  "description": "Visual layout tests for portfolio website",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report tests/report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
